import { RippleDirectiveDirective } from './ripple-directive.directive';

describe('RippleDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new RippleDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
